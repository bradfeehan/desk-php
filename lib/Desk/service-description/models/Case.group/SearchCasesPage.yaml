extends: page
properties:
    entries:
        type: array
        items: { extends: CaseModel }
        location: embedded
    self:
        location: links
        data:
            operation: SearchCases
            pattern: "#/cases/search\\??(?P<_query>.*)$#"
    first:
        location: links
        data:
            operation: SearchCases
            pattern: "#/cases/search\\??(?P<_query>.*)$#"
    last:
        location: links
        data:
            operation: SearchCases
            pattern: "#/cases/search\\??(?P<_query>.*)$#"
    next:
        location: links
        data:
            operation: SearchCases
            pattern: "#/cases/search\\??(?P<_query>.*)$#"
    previous:
        location: links
        data:
            operation: SearchCases
            pattern: "#/cases/search\\??(?P<_query>.*)$#"
