extends: page
properties:
    entries:
        type: array
        items: { extends: GroupModel }
        location: embedded
    self:
        location: links
        data:
            operation: ListGroups
            pattern: "#/groups\\??(?P<_query>.*)$#"
    first:
        location: links
        data:
            operation: ListGroups
            pattern: "#/groups\\??(?P<_query>.*)$#"
    last:
        location: links
        data:
            operation: ListGroups
            pattern: "#/groups\\??(?P<_query>.*)$#"
    next:
        location: links
        data:
            operation: ListGroups
            pattern: "#/groups\\??(?P<_query>.*)$#"
    previous:
        location: links
        data:
            operation: ListGroups
            pattern: "#/groups\\??(?P<_query>.*)$#"
