extends: page
properties:
    entries:
        type: array
        items: { extends: UserModel }
        location: embedded
    self:
        location: links
        data:
            operation: ListUsers
            pattern: "#/users\\??(?P<_query>.*)$#"
    first:
        location: links
        data:
            operation: ListUsers
            pattern: "#/users\\??(?P<_query>.*)$#"
    last:
        location: links
        data:
            operation: ListUsers
            pattern: "#/users\\??(?P<_query>.*)$#"
    next:
        location: links
        data:
            operation: ListUsers
            pattern: "#/users\\??(?P<_query>.*)$#"
    previous:
        location: links
        data:
            operation: ListUsers
            pattern: "#/users\\??(?P<_query>.*)$#"
